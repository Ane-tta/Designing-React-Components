import React from 'react'

// At the top of this file, let's define a context. We do that by simply assigning to
// a constant the return of `React.createContext`
const SpeakerContext = React.createContext({});

export default SpeakerContext;